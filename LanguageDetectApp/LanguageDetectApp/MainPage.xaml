<Page
    x:Class="LanguageDetectApp.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:LanguageDetectApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <StackPanel>
            <Frame x:Name="ScenarioFrame" />
            <Image Name="imageView" 
                   Source="{Binding Image}"
                   Height="480" Width="400" HorizontalAlignment="Center"/>
            
            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                <Button Name="browsebtn" Content="Browse" HorizontalAlignment="Center" Click="BrowseButton_Click"/>
                <Button Name="wtndection" Content="WTN" HorizontalAlignment="Center" Click="WhatTheNumberButton_Click"/>
            </StackPanel>
        </StackPanel>

    </Grid>
    
</Page>
